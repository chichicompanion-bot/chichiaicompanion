# Project Structure

Du an da duoc tach thanh hai khu de de quan ly:

- `chatbot/`: bot Python, runner, config, tests
- `web/`: frontend web rieng

Chay chatbot:

```powershell
cd chatbot
pip install -r requirements.txt
python dev_runner.py
```

Mo web:

```powershell
cd web
start index.html
```
